% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design.R
\name{build_design}
\alias{build_design}
\title{Build the design used by both arms}
\usage{
build_design(formula, meta, tested_term, depth)
}
\arguments{
\item{formula}{RHS-only formula, e.g. \code{~ group + age}.}

\item{meta}{sample metadata (data.frame), rows aligned to the count table.}

\item{tested_term}{single term label present in \code{formula} (e.g. \code{"group"}).}

\item{depth}{numeric vector of per-sample library sizes.}
}
\value{
list with \code{X} (model matrix, intercept first), \code{tested_cols} (integer
indices of the tested term's columns in \code{X}), \code{nuisance_cols}, \code{log_depth}
(centred), \code{term_labels}, and \code{coef_names}.
}
\description{
Expands \code{formula} on \code{meta}, identifies the columns that belong to \code{tested_term},
and returns everything the arms need. The abundance arm appends a centred
log-depth column; the presence arm uses the depth as an offset instead.
}
\keyword{internal}
