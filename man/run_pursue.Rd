% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pursue_main.R
\name{run_pursue}
\alias{run_pursue}
\title{Run the PURSUE workflow}
\usage{
run_pursue(
  otu,
  meta,
  formula,
  tested_term,
  tested_vars = NULL,
  taxa_keep = NULL,
  min_prevalence_filter = NULL,
  min_total_count_filter = NULL,
  reference_pseudocount = 1,
  ref_n_max = 100000000L,
  bootstrap_B = 100L,
  bootstrap_stability_threshold = 0.8,
  minimum_TA = 10,
  minimum_rel_TA = 0.01,
  hostage_prevention = TRUE,
  bulk_rel_quantile = 0.1,
  low_count_floor = 5L,
  tail_zero_tol = 1L,
  tail_low_prop_tol = 0.05,
  hostage_max_size_inflation = 0.5,
  hostage_max_score_inflation = 0.25,
  hostage_max_stability_drop = 0.05,
  reference_group_cleanup = TRUE,
  reference_group_cleanup_frac = 0.1,
  reference_group_cleanup_min_ref = 10L,
  reference_group_cleanup_pseudocount = 1,
  depth_adjust = TRUE,
  prevalence_expected_rarefied_eps = 1e-12,
  parallelize_permutations = FALSE,
  permutation_n_cores = max(1L, parallel::detectCores() - 1L),
  abundance_min_nonzero_n = 3L,
  abund_resid_winsorize = TRUE,
  abund_resid_winsor_quantile = 0.05,
  abund_resid_winsor_min_n = 20L,
  n_perm = 1999,
  cluster_id = NULL,
  strata = NULL,
  cluster_exposure_mode = c("auto", "sample", "cluster"),
  seed = NULL,
  union_acat_weights = c(0.5, 0.5),
  independent_filtering = FALSE,
  independent_filter_alpha = 0.05,
  independent_filter_theta_grid = seq(0, 0.8, by = 0.02),
  independent_filter_filtered_q_value = NA_real_,
  keep_diagnostics = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{otu}{A feature count matrix. Rows correspond to samples and columns
correspond to microbial features.}

\item{meta}{A sample metadata data frame. Rows must correspond to the samples
in \code{otu}.}

\item{formula}{A model formula specifying the tested term and any nuisance
covariates.}

\item{tested_term}{A single term from \code{formula} to be tested.}

\item{tested_vars}{Optional character vector specifying the variable or
variables to be permuted in the Freedman--Lane scheme. If \code{NULL}, defaults
to \code{tested_term}.}

\item{taxa_keep}{Optional full length logical or integer index specifying features to
retain before model fitting. Default is \code{NULL}.}

\item{min_prevalence_filter}{Optional prevalence threshold for filtering
low-prevalence features. Default is \code{NULL}.}

\item{min_total_count_filter}{Optional total-count threshold for filtering
low-count features. Default is \code{NULL}.}

\item{reference_pseudocount}{Pseudocount used in log-ratio reference scoring.}

\item{ref_n_max}{Maximum number of candidate reference features considered
during reference scoring.}

\item{bootstrap_B}{Number of bootstrap resamples used for bootstrap-prefix
reference selection.}

\item{bootstrap_stability_threshold}{Minimum prefix stability required for
bootstrap-prefix reference selection.}

\item{minimum_TA}{Minimum total reference abundance threshold used in
bootstrap-prefix reference selection. Default is \code{10}.}

\item{minimum_rel_TA}{Minimum relative reference abundance threshold used in
bootstrap-prefix reference selection. Default is \code{0.01}.}

\item{hostage_prevention}{Logical; if \code{TRUE}, extends the selected reference
prefix when needed to reduce fragile low-mass tails.}

\item{bulk_rel_quantile}{Quantile used when evaluating relative reference
adequacy.}

\item{low_count_floor}{Count threshold used in bootstrap adequacy and hostage
prevention diagnostics.}

\item{tail_zero_tol}{Maximum tolerated number of zero reference-mass samples
in hostage prevention.}

\item{tail_low_prop_tol}{Maximum tolerated proportion of low reference-mass
samples in hostage prevention.}

\item{hostage_max_size_inflation}{Maximum allowed reference-set size inflation
during hostage prevention.}

\item{hostage_max_score_inflation}{Maximum allowed score inflation during
hostage prevention.}

\item{hostage_max_stability_drop}{Maximum allowed stability loss during
hostage prevention.}

\item{reference_group_cleanup}{Logical; if \code{TRUE}, performs post-selection
group-shift cleanup of the reference set.}

\item{reference_group_cleanup_frac}{Fraction of selected references removed
during group-shift cleanup.}

\item{reference_group_cleanup_min_ref}{Minimum number of references retained
after group-shift cleanup.}

\item{reference_group_cleanup_pseudocount}{Pseudocount used during
group-shift cleanup.}

\item{depth_adjust}{Logical; if \code{TRUE}, appends \code{log(depth)} to the model
formula. Default is \code{TRUE}.}

\item{prevalence_expected_rarefied_eps}{Numerical clipping constant used in
expected-rarefied prevalence construction.}

\item{parallelize_permutations}{Logical; if \code{TRUE}, permutations are
parallelized.}

\item{permutation_n_cores}{Number of cores used when
\code{parallelize_permutations = TRUE}.}

\item{abundance_min_nonzero_n}{Minimum number of observed nonzero abundance
values required to fit an abundance model for a feature.}

\item{abund_resid_winsorize}{Logical; if \code{TRUE}, winsorizes abundance-model
residuals in the permutation engine.}

\item{abund_resid_winsor_quantile}{Quantile used for residual winsorization.}

\item{abund_resid_winsor_min_n}{Minimum sample size required before residual
winsorization is applied.}

\item{n_perm}{Number of permutations used for empirical p-value estimation.}

\item{cluster_id}{Optional clustering variable for clustered permutation.
Useful for repeated-measures or paired designs. Default is \code{NULL}.}

\item{strata}{Optional stratification variable restricting permutations to
occur within strata. Default is \code{NULL}.}

\item{cluster_exposure_mode}{Controls whether permutation is performed at the
sample or cluster level. One of \code{"auto"}, \code{"sample"}, or \code{"cluster"}.}

\item{seed}{Optional random seed.}

\item{union_acat_weights}{Numeric vector of length two giving prevalence and
abundance weights for ACAT union p-value combination.}

\item{independent_filtering}{Logical; if \code{TRUE}, applies adaptive independent
filtering before multiple-testing correction.}

\item{independent_filter_alpha}{Target FDR level used by adaptive independent
filtering.}

\item{independent_filter_theta_grid}{Candidate filtering thresholds used by
the adaptive independent filtering procedure.}

\item{independent_filter_filtered_q_value}{Q-value assigned to filtered-out
features.}

\item{keep_diagnostics}{Logical; if \code{TRUE}, returns intermediate diagnostics
in addition to the main results table.}

\item{verbose}{Logical; if \code{TRUE}, prints progress messages.}
}
\value{
If \code{keep_diagnostics = FALSE}, a list with \code{results_table}. If
\code{keep_diagnostics = TRUE}, a list containing the final \code{results_table} plus
the filter object, reference-selection output, constructed responses,
observed component fits, permutation outputs, and union-layer output.
}
\description{
\code{run_pursue()} fits the full PURSUE workflow for microbial differential
abundance analysis. The procedure filters low-information features,
selects a stable reference set, constructs reference-normalized prevalence and
abundance responses, fits the two model components separately, computes
empirical p-values using a Freedman--Lane residual permutation scheme,
optionally applies residual winsorization within the abundance permutation
engine, and combines component-level evidence into union-level results with
optional adaptive independent filtering.
}
\details{
PURSUE models microbial differential signals using two reference-normalized
components: a prevalence component and an abundance component. Reference
features are selected before response construction, after which the two
components are fitted separately and evaluated using empirical p-values from
a Freedman--Lane residual permutation scheme. Component-level evidence is
then combined into union-level results, with optional adaptive independent
filtering before multiple-testing correction.
}
\examples{
set.seed(1)

otu <- matrix(
  c(
    12, 0, 3, 5,
     8, 1, 2, 4,
    15, 0, 1, 6,
     3, 7, 0, 8,
     2, 9, 1, 10,
     1, 11, 0, 9
  ),
  nrow = 6,
  byrow = TRUE
)

colnames(otu) <- paste0("Taxon_", seq_len(ncol(otu)))
rownames(otu) <- paste0("Sample_", seq_len(nrow(otu)))

meta <- data.frame(
  group = factor(c("A", "A", "A", "B", "B", "B")),
  row.names = rownames(otu)
)

out <- run_pursue(
  otu = otu,
  meta = meta,
  formula = ~ group,
  tested_term = "group",
  n_perm = 99,
  verbose = TRUE
)

head(out$results_table)

}
